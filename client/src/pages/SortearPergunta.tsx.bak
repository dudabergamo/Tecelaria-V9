import { useAuth } from "@/_core/hooks/useAuth";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { trpc } from "@/lib/trpc";
import { 
  ArrowLeft, 
  Dices, 
  Mic, 
  PenLine, 
  Sparkles 
} from "lucide-react";
import { useState } from "react";
import { Link, useLocation } from "wouter";

export default function SortearPergunta() {
  const [, setLocation] = useLocation();
  const { user, loading } = useAuth();
  const [selectedBox, setSelectedBox] = useState<number | undefined>(undefined);
  const [currentQuestion, setCurrentQuestion] = useState<any>(null);
  const [shouldFetch, setShouldFetch] = useState(false);
  
  const { data: questionData, isFetching } = trpc.questions.getRandomQuestion.useQuery(
    { boxNumber: selectedBox },
    { 
      enabled: shouldFetch,
    }
  );

  // Update current question when data changes
  if (questionData && questionData !== currentQuestion && !isFetching) {
    setCurrentQuestion(questionData);
    setShouldFetch(false);
  }

  const handleSortear = () => {
    setShouldFetch(true);
  };

  const boxes = [
    { 
      number: 1, 
      name: "Comece Por Aqui", 
      description: "Perguntas básicas e cadastrais",
      color: "from-green-500/10 to-green-500/5 border-green-500/20"
    },
    { 
      number: 2, 
      name: "Siga Por Aqui", 
      description: "Perguntas desejáveis sobre gostos e preferências",
      color: "from-blue-500/10 to-blue-500/5 border-blue-500/20"
    },
    { 
      number: 3, 
      name: "Lembranças Profundas", 
      description: "Perguntas amplas e reflexivas",
      color: "from-purple-500/10 to-purple-500/5 border-purple-500/20"
    },
    { 
      number: 4, 
      name: "Detalhes que Contam", 
      description: "Perguntas específicas sobre lugares e pessoas",
      color: "from-orange-500/10 to-orange-500/5 border-orange-500/20"
    },
  ];

  if (loading) {
    return (
      <div className="min-h-screen flex items-center justify-center">
        <div className="text-center space-y-4">
          <div className="animate-spin rounded-full h-12 w-12 border-b-2 border-primary mx-auto"></div>
          <p className="text-muted-foreground">Carregando...</p>
        </div>
      </div>
    );
  }

  return (
    <div className="min-h-screen bg-background">
      {/* Header */}
      <header className="border-b bg-card">
        <div className="container py-4">
          <div className="flex items-center justify-between">
            <Link href="/">
              <h1 className="text-2xl font-bold cursor-pointer hover:text-primary transition-colors">
                Tecelaria
              </h1>
            </Link>
            <div className="flex items-center gap-4">
              <Button variant="outline" size="sm" asChild>
                <Link href="/dashboard">
                  <ArrowLeft className="mr-2 h-4 w-4" />
                  Voltar ao Dashboard
                </Link>
              </Button>
            </div>
          </div>
        </div>
      </header>

      <div className="container py-8 space-y-8 max-w-4xl">
        {/* Title */}
        <div className="space-y-2 text-center">
          <h2 className="text-3xl font-bold flex items-center justify-center gap-2">
            <Dices className="h-8 w-8 text-primary" />
            Sortear Pergunta
          </h2>
          <p className="text-muted-foreground">
            Escolha uma caixinha ou deixe em branco para sortear de todas
          </p>
        </div>

        {/* Box Selection */}
        <div className="grid md:grid-cols-2 gap-4">
          <Card 
            className={`cursor-pointer transition-all hover:scale-105 ${
              selectedBox === undefined 
                ? 'border-2 border-primary shadow-lg' 
                : 'border-2 border-transparent hover:border-muted'
            }`}
            onClick={() => setSelectedBox(undefined)}
          >
            <CardContent className="pt-6 text-center">
              <Sparkles className="h-8 w-8 mx-auto mb-2 text-primary" />
              <h3 className="font-bold">Todas as Caixinhas</h3>
              <p className="text-sm text-muted-foreground mt-1">
                Surpresa total! 150 perguntas
              </p>
            </CardContent>
          </Card>

          {boxes.map((box) => (
            <Card
              key={box.number}
              className={`cursor-pointer transition-all hover:scale-105 bg-gradient-to-br ${box.color} ${
                selectedBox === box.number 
                  ? 'border-2 shadow-lg scale-105' 
                  : 'border-2 hover:border-muted'
              }`}
              onClick={() => setSelectedBox(box.number)}
            >
              <CardContent className="pt-6">
                <div className="flex items-start gap-3">
                  <div className="w-10 h-10 rounded-full bg-background/50 flex items-center justify-center font-bold">
                    {box.number}
                  </div>
                  <div className="flex-1">
                    <h3 className="font-bold">{box.name}</h3>
                    <p className="text-sm text-muted-foreground mt-1">
                      {box.description}
                    </p>
                  </div>
                </div>
              </CardContent>
            </Card>
          ))}
        </div>

        {/* Sortear Button */}
        <div className="text-center">
          <Button 
            size="lg" 
            className="text-lg px-8 py-6 rounded-xl"
            onClick={handleSortear}
            disabled={isFetching}
          >
            {isFetching ? (
              <>
                <div className="animate-spin rounded-full h-5 w-5 border-b-2 border-white mr-2"></div>
                Sorteando...
              </>
            ) : (
              <>
                <Dices className="mr-2 h-5 w-5" />
                Sortear Pergunta
              </>
            )}
          </Button>
        </div>

        {/* Question Display */}
        {currentQuestion && (
          <Card className="border-2 border-primary shadow-xl animate-in fade-in slide-in-from-bottom-4 duration-500">
            <CardHeader className="text-center border-b bg-gradient-to-br from-primary/5 to-transparent">
              <div className="inline-block px-3 py-1 rounded-full bg-primary/10 text-primary text-sm font-semibold mb-2">
                Caixinha {currentQuestion.box}: {currentQuestion.boxName}
              </div>
              <CardTitle className="text-2xl">
                Pergunta #{currentQuestion.number}
              </CardTitle>
            </CardHeader>
            <CardContent className="pt-8 pb-8">
              <p className="text-xl text-center font-medium mb-8 leading-relaxed">
                {currentQuestion.question}
              </p>

              <div className="flex flex-col sm:flex-row gap-3 justify-center">
                <Button size="lg" variant="default" asChild>
                  <Link href={`/registrar-memoria?pergunta=${encodeURIComponent(currentQuestion.question)}&questionId=${currentQuestion.id}`}>
                    <Mic className="mr-2 h-5 w-5" />
                    Gravar Áudio
                  </Link>
                </Button>
                <Button size="lg" variant="outline" asChild>
                  <Link href={`/registrar-memoria?pergunta=${encodeURIComponent(currentQuestion.question)}&questionId=${currentQuestion.id}`}>
                    <PenLine className="mr-2 h-5 w-5" />
                    Escrever Texto
                  </Link>
                </Button>
              </div>
            </CardContent>
          </Card>
        )}

        {/* Empty State */}
        {!currentQuestion && (
          <Card className="border-dashed border-2">
            <CardContent className="pt-12 pb-12 text-center">
              <Dices className="h-16 w-16 text-muted-foreground mx-auto mb-4 opacity-50" />
              <p className="text-muted-foreground">
                Clique em "Sortear Pergunta" para começar
              </p>
            </CardContent>
          </Card>
        )}

        {/* Info Box */}
        <Card className="bg-muted/50">
          <CardContent className="pt-6">
            <h3 className="font-bold mb-2">💡 Dica</h3>
            <p className="text-sm text-muted-foreground">
              As perguntas são "abridoras de caixinhas" - use-as como inspiração, mas você não precisa seguir um roteiro rígido. 
              Sinta-se livre para gravar memórias sobre qualquer assunto!
            </p>
          </CardContent>
        </Card>
      </div>
    </div>
  );
}
